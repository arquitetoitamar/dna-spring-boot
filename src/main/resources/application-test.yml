spring:
  application:
    name: "Stats Dna"
  data:
    neo4j:
      database: ${NEO4J_DATABASE:neo4j}
server:
  port: ${PORT:8080}

logging:
  level:
    org:
      springframework:
        data:
          neo4j:
            cypher: trace